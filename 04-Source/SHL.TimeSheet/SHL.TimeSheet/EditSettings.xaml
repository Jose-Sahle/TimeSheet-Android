<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="SHL.TimeSheet.EditSettings"
             Title="Edit Settings">
    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="Undo_32.png" Clicked="cmdBack_Click" />
        <ToolbarItem IconImageSource="Check_32.png" Clicked="cmdFinish_Click" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Margin="2">
            <Editor Placeholder="Entre com o nome da chave"
                Text="{Binding key}"
                HeightRequest="100" />

            <Editor Placeholder="Entre com o conteúdo do valor da chave"
                Text="{Binding value}"
                HeightRequest="100" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>